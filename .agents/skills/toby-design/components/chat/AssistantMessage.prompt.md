The answer. Toby's one deliberate typographic break: assistant prose is set in the system **serif** at 15px with generous leading, so the response reads as a document while all chrome stays rounded SF.

```jsx
<AssistantMessage header="Toby" avatarSrc="assets/personas/toby.png">
  <p>Three threads need a reply today…</p>
</AssistantMessage>
```

Max 640px wide, left-aligned, with a hairline rail under the avatar.
