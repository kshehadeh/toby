The composer. It floats over the transcript on the content background with a 16px radius and the only large shadow in the app.

```jsx
<InputDock value={draft} contextPercent={42} onChange={setDraft} onSubmit={send} />
```

The Send button inverts (solid primary text, content-colored glyph) once there is something to send; disabled it is a neutral selection wash.
