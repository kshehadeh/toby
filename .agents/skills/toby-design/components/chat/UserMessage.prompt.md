The user's turn: right-aligned, max 520px, elevated fill, 14px radius, accent bar on the leading edge. Rounded SF at body size.

```jsx
<UserMessage text="Summarize unread mail that needs a reply" footer={<CopyLink />} />
```

Prompts longer than 12 lines collapse with a "Show more" link.
