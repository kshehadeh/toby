Uppercase capsule for status labels. Toby uses it sparingly — one per surface at most.

```jsx
<Badge tone="accent">Up next</Badge>
<Badge>Updating</Badge>
```

`accent` is a solid accent fill with a near-black label; `neutral` is a faint outlined capsule; `accentSoft` is the tinted-wash variant.
