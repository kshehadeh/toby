Pill-shaped attachment chip used above the chat input and in transcript rows.

```jsx
<Chip leading={<Paperclip />} label="Q3-forecast.xlsx" meta="182 KB" onRemove={remove} />
```

Fills with `--surface-selected`; text is muted with a faint size suffix. Never colored by state.
