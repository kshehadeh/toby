Loading placeholder for dashboard card bodies and detail panes. Elevated-surface bars that pulse 0.9 → 0.5 over 800ms, autoreversing.

```jsx
{isUpdating ? <Skeleton lines={4} /> : <Summary … />}
```
