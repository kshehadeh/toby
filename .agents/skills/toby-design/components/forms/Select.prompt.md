Menu-style picker for enumerable settings (theme, accent, provider, model).

```jsx
<Select value={mode} options={[{value:'system',label:'System'},{value:'light',label:'Light'},{value:'dark',label:'Dark'}]} onChange={setMode} />
```

Labels are hidden — the SettingsRow title is the label.
