Selectable sidebar row. Selection is a neutral 6–8% wash plus a promotion from muted to primary text — never an accent fill.

```jsx
<SidebarRow title="Jira triage" subtitle="Yesterday" glyph={<img src="assets/icons/integrations/jira.png" width="16" />} selected />
```
