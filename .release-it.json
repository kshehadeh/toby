{
	"$schema": "https://unpkg.com/release-it@20/schema/release-it.json",
	"npm": {
		"publish": false
	},
	"github": false,
	"git": {
		"commitMessage": "chore(release): v${version}",
		"tagName": "v${version}",
		"tagAnnotation": "Release v${version}"
	},
	"hooks": {
		"after:bump": "node -e \"const v=require('./package.json').version; const fs=require('fs'); const p='apps/cli/package.json'; const j=JSON.parse(fs.readFileSync(p,'utf8')); j.version=v; fs.writeFileSync(p, JSON.stringify(j,null,'\\t')+'\\n');\" && bun install && git add bun.lock apps/cli/package.json"
	}
}
