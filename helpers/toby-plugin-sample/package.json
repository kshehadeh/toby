{
	"name": "toby-plugin-sample",
	"version": "1.0.0",
	"private": true,
	"type": "module",
	"description": "Reference installable Toby integration plugin (protocol v1)",
	"scripts": {
		"dev": "bun src/cli.ts",
		"build": "bun build ./src/cli.ts --compile --outfile ../../dist/toby-plugin-sample"
	}
}
