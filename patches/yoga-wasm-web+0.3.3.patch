diff --git a/node_modules/yoga-wasm-web/dist/ink-default.js b/node_modules/yoga-wasm-web/dist/ink-default.js
new file mode 100644
index 0000000..b00765f
--- /dev/null
+++ b/node_modules/yoga-wasm-web/dist/ink-default.js
@@ -0,0 +1,11 @@
+/**
+ * Ink expects the same default shape as `yoga-wasm-web/auto` on Node: the
+ * initialized Yoga API object. The `asm` entrypoint only exports the init
+ * function; this module invokes it once and re-exports the result so all Ink
+ * importers share one instance via the package export map.
+ */
+import initYoga from "./asm.js";
+
+const Yoga = initYoga();
+
+export default Yoga;
